{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    // ALLOWING JS WILL HELP TO IMPORT JS FILES IN TS PROJECTS
    // BUT IT MAY CAUSE SOME TYPE SAFETY ISSUES
    // SO USE IT WITH CAUTION AND ONLY IF NECESSARY
    // FOR EXAMPLE, WHEN MIGRATING A JS PROJECT TO TS
    // OR WHEN USING THIRD-PARTY JS LIBRARIES WITHOUT TYPE DEFINITIONS
    // IF YOU DON'T NEED TO IMPORT JS FILES, CONSIDER SETTING THIS TO FALSE
    //  TO ENSURE BETTER TYPE SAFETY IN YOUR PROJECT
    // YOU CAN ALSO USE @ts-ignore OR DECLARE MODULES FOR SPECIFIC JS FILES IF NEEDED
    // REMEMBER TO REVIEW YOUR CODE REGULARLY TO AVOID UNINTENDED TYPE ISSUES
    // THIS SETTING IS A TRADE-OFF BETWEEN CONVENIENCE AND TYPE SAFETY
    "allowJs": true
  },
  "include": ["src"]
}
